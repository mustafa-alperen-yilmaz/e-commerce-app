import { EcommerceShopvalidators } from './ecommerce-shopvalidators';

describe('EcommerceShopvalidators', () => {
  it('should create an instance', () => {
    expect(new EcommerceShopvalidators()).toBeTruthy();
  });
});
